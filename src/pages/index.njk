---
layout: layouts/page.njk
title: Home
permalink: /
---

<!-- Hero Section -->
<section class="relative h-[600px] flex items-center" style="background: url('/img/hero-home.jpg') center/cover no-repeat;">
  <!-- Subtle dark overlay -->
  <div class="absolute inset-0 bg-black opacity-10"></div>

  <!-- White gradient overlay - lighter on left, transparent on right -->
  <div class="absolute inset-0 bg-gradient-to-r from-white/40 via-white/20 to-transparent"></div>

  <div class="relative z-10 w-full">
    <div class="flex items-center gap-8 max-w-4xl mx-auto px-8">
      <!-- Profile Photo - Left Side -->
      <img src="/img/profile.jpg" alt="Jeremy Ricketts" class="w-48 h-48 rounded-full border-4 border-white shadow-lg object-cover flex-shrink-0">

      <!-- Text Content - Right Side -->
      <div class="text-left flex-1">
        <h1 class="text-4xl md:text-5xl font-bold mb-4 leading-tight">
          <span class="text-black">Fun and practical</span><br>
          <span class="text-[#5a8fb4]">dog training</span>
          <span class="text-black"> in North County San Diego</span>
        </h1>

        <h2 class="text-xl md:text-2xl text-black" style="font-family: var(--font-family-tagline)">
          Hi, I'm Jeremy and I'm here to help you have a stronger bond with your dog through personal, skill-focused training lessons. You can do this!
        </h2>
      </div>
    </div>
  </div>
</section>

<!-- Services Grid -->
<section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    {% for service in services %}
    <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow">
      <img src="https://cdnjs.cloudflare.com/ajax/libs/twemoji/14.0.2/svg/{{ service.icon }}.svg" alt="{{ service.title }}" class="w-12 h-12 mb-4">
      <h3 class="text-xl font-bold mb-3" style="color: var(--color-primary-green)">{{ service.title }}</h3>
      <p class="text-gray-700">{{ service.description | safe }}</p>
    </div>
    {% endfor %}
  </div>
</section>

<!-- Skill-Focused Lessons Banner -->
<section class="relative py-24 text-center" style="background: linear-gradient(rgba(106, 134, 90, 0.9), rgba(106, 134, 90, 0.9)), url('/img/hero-lessons.jpg') center/cover;">
  <div class="max-w-4xl mx-auto px-4">
    <h2 class="text-4xl font-bold text-white mb-4">Skill-Focused Lessons</h2>
    <p class="text-xl text-white mb-8">Each lesson builds on the next. We can always slow down, speed up, or revisit skills as necessary.</p>
    <a href="/lessons/" class="btn-primary inline-block">View the Lessons</a>
  </div>
</section>

<!-- Cost / Location / Availability -->
<section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
    <div class="text-center">
      <h2 class="text-3xl font-bold mb-4" style="color: var(--color-primary-green)">Cost</h2>
      <p class="text-lg text-gray-700">
        I charge $40 per 45 minute one-on-one training sessions. Payments can be made by cash or
        <a href="#payment" class="underline hover:text-[color:var(--color-accent-green)]">digitally</a>.
      </p>
    </div>
    <div class="text-center">
      <h2 class="text-3xl font-bold mb-4" style="color: var(--color-primary-green)">Location</h2>
      <p class="text-lg text-gray-700">
        I rotate between <a href="/locations/" class="underline hover:text-[color:var(--color-accent-green)]">a few parks</a> here in North County San Diego that work great for outdoor dog training.
      </p>
    </div>
    <div class="text-center">
      <h2 class="text-3xl font-bold mb-4" style="color: var(--color-primary-green)">Availability</h2>
      <p class="text-lg text-gray-700">
        Sessions are available on Sunday mornings from 8am through 1pm and can be
        <a href="/booking-contact/" class="underline hover:text-[color:var(--color-accent-green)]">booked here</a>.
      </p>
    </div>
  </div>
</section>

<!-- Testimonials -->
<section class="bg-gray-100 py-16">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      {% for testimonial in testimonials %}
      <div class="testimonial-card">
        <div class="flex items-start mb-4">
          <img src="{{ testimonial.image }}" alt="{{ testimonial.name }}" class="w-16 h-16 rounded-full object-cover mr-4">
          <div>
            <h3 class="font-bold text-lg">{{ testimonial.name }}</h3>
            <p class="text-sm text-gray-600">{{ testimonial.location }}</p>
          </div>
        </div>
        <p class="italic text-gray-700">{{ testimonial.text }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

