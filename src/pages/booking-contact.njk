---
layout: layouts/page.njk
title: Booking and Contact
permalink: /booking-contact/
---

<!-- Hero Section -->
<section class="relative h-[400px] flex items-center justify-center text-center" style="background: linear-gradient(rgba(106, 134, 90, 0.85), rgba(106, 134, 90, 0.85)), url('/img/hero-lessons.jpg') center/cover;">
  <div class="relative z-10 max-w-4xl mx-auto px-4">
    <h1 class="text-5xl font-bold text-white">Booking & Contact Info</h1>
  </div>
</section>

<!-- Intake Form CTA -->
<section class="bg-gray-100 py-12">
  <div class="max-w-2xl mx-auto px-4 text-center">
    <p class="text-lg text-gray-700 mb-4">
      If we've never trained before, please take a moment to tell me about yourself and your dog.
    </p>
    <button onclick="document.getElementById('intakeModal').showModal()" class="btn-primary">
      Intake Form
    </button>
  </div>
</section>

<!-- Contact Info -->
<section class="py-16">
  <div class="max-w-3xl mx-auto px-4 text-center">
    <h2 class="text-3xl font-bold mb-2" style="color: var(--color-primary-green)">{{ site.phone }}</h2>
    <h2 class="text-3xl font-bold mb-8" style="color: var(--color-primary-green)">{{ site.email }}</h2>

    <div class="flex justify-center gap-4 mb-6">
      <a href="sms:{{ site.phone }}" class="inline-flex items-center gap-2 btn-primary">
        <img src="https://cdnjs.cloudflare.com/ajax/libs/twemoji/14.0.2/svg/1f4f1.svg" alt="Phone" class="w-5 h-5">
        Text
      </a>
      <a href="tel:{{ site.phone }}" class="inline-flex items-center gap-2 btn-primary">
        <img src="https://cdnjs.cloudflare.com/ajax/libs/twemoji/14.0.2/svg/1f4de.svg" alt="Call" class="w-5 h-5">
        Call
      </a>
      <a href="mailto:{{ site.email }}" class="inline-flex items-center gap-2 btn-primary">
        <img src="https://cdnjs.cloudflare.com/ajax/libs/twemoji/14.0.2/svg/1f4e7.svg" alt="Email" class="w-5 h-5">
        Email
      </a>
    </div>

    <p class="text-gray-600">I'm perfectly comfortable texting if you are!</p>
  </div>
</section>

<!-- Calendly Booking -->
<section class="bg-gray-50 py-16">
  <div class="max-w-4xl mx-auto px-4">
    <h2 class="text-3xl font-bold text-center mb-8" style="color: var(--color-primary-green)">
      Book a Sunday Morning Session:
    </h2>
    <div class="iframe-container" style="padding-top: 75%; min-height: 600px;">
      <iframe
        src="{{ site.calendly_url }}"
        width="100%"
        height="100%"
        frameborder="0"
        title="Book a training session">
      </iframe>
    </div>
  </div>
</section>

<!-- Intake Form Modal -->
<dialog id="intakeModal" class="rounded-lg shadow-2xl max-w-2xl w-full p-0 backdrop:bg-black backdrop:opacity-50">
  <div class="bg-white rounded-lg">
    <div class="flex justify-between items-center p-6 border-b">
      <h3 class="text-2xl font-bold" style="color: var(--color-primary-green)">Intake Form</h3>
      <button onclick="document.getElementById('intakeModal').close()" class="text-gray-500 hover:text-gray-700 text-3xl leading-none">
        &times;
      </button>
    </div>

    <form action="https://submit-form.com/{{ site.formspark_id }}" method="POST" class="p-6">
      <div class="space-y-4">
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Your Name *</label>
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[color:var(--color-primary-green)] focus:border-transparent"
          >
        </div>

        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Your Email *</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[color:var(--color-primary-green)] focus:border-transparent"
          >
        </div>

        <div>
          <label for="message" class="block text-sm font-medium text-gray-700 mb-1">About you and your dog *</label>
          <textarea
            id="message"
            name="message"
            required
            rows="6"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[color:var(--color-primary-green)] focus:border-transparent"
            placeholder="Tell me about your dog's age, breed, temperament, and what you'd like to work on..."></textarea>
        </div>

        <button type="submit" class="btn-primary w-full">
          Submit
        </button>
      </div>
    </form>
  </div>
</dialog>
